# ARISUINFO 기존 대비 권한·메뉴·공통처리 보강 명세

> 전체 이관으로 목표가 확대됐습니다. 이 문서는 기존 샘플 기준 비교이며 최신 범위는
> [전체 이관 기준](MIGRATION_SCOPE.md), 추가 구현과 남은 작업은 [이관 현황](migration/IMPLEMENTATION_STATUS.md)을 참조하세요.

## 1. 문서 목적

이 문서는 ARISUINFO가 기존에 처리하던 인증·권한·메뉴·프로그램 기능·공통코드 방식을 기준으로, 독립 프로젝트 `permission-menu-demo`에서 무엇을 유지하고 무엇을 보강했는지 명확히 구분하기 위한 문서입니다.

다음 질문에 답하는 것을 목표로 합니다.

- INFO가 원래 가지고 있던 업무 규칙은 무엇인가?
- INFO에서 확인된 보안·구조·정합성 제약은 무엇인가?
- 독립 프로젝트에서 실제로 보강해 구현한 것은 무엇인가?
- 문서로만 설계됐고 아직 구현되지 않은 것은 무엇인가?
- INFO보다 구조는 강화됐지만 업무 범위가 줄어든 부분은 무엇인가?

상세 원본 경로와 분석 근거는 [ARISUINFO 권한 분석 문서](ARISUINFO_PERMISSION_ANALYSIS.md)를 함께 참조합니다.

## 2. 상태 표기

| 상태 | 의미 |
|---|---|
| `INFO 기존` | ARISUINFO에서 이미 사용 중인 기능 또는 업무 규칙 |
| `구현 완료` | 독립 프로젝트 소스와 자동 테스트에 반영된 보강 내용 |
| `설계 완료` | 문서와 모델은 준비됐지만 실행 소스나 DB에는 아직 미반영 |
| `미구현` | 향후 개발이 필요한 기능 |
| `단순화` | 독립 테스트 목적상 INFO보다 범위를 줄인 부분 |

## 3. 최종 결론

독립 프로젝트는 INFO를 그대로 복사한 시스템이 아닙니다.

```text
INFO의 업무 권한 개념과 데이터 관계
  + 서버 중심 신뢰 경계
  + 위임 원천 검증
  + 메뉴·프로그램·기능 문맥 인가
  + API별 서버 재인가
  + CSRF·표준 오류·traceId·감사
  + 자동 테스트
  - Nexacro 전용 처리
  - 현재 단계의 Oracle·SSO·전체 관리 CRUD
```

따라서 현재 상태는 다음처럼 표현하는 것이 정확합니다.

> 권한 판정과 보안 구조는 INFO보다 보강됐지만, Oracle 영속화·SSO·권한 및 메뉴 전체 관리 기능은 아직 INFO보다 범위가 작다.

## 4. 전체 비교표

| 영역 | INFO 기존 내용 | 독립 프로젝트 보강 내용 | 현재 상태 |
|---|---|---|---|
| 인증 | SSO 인증 후 공통 Spring 역할 부여 | Spring 역할과 업무 권한을 명시적으로 분리 | 구현 완료 |
| 현재 사용자 | `SsoUserDetails`, 세션 `LoginVO` 사용 | `SecurityContext` 사용자명만 신뢰 | 구현 완료 |
| 현재 조직 | SSO의 사업소·부서와 권한 조직 비교 | 서버 사용자 프로필의 현재 조직으로만 계산 | 구현 완료·단순화 |
| 직접 권한 | 최신 승인 상태와 기간으로 조회 | 승인·기간·현재 조직·권한 활성까지 공통 검사 | 구현 완료 |
| 비활성 권한 | 유효 권한 결과에 남을 가능성 존재 | 권한 마스터가 비활성이면 항상 제외 | 구현 완료 |
| 위임 | 유효 기간 위임을 직접 권한과 합산 | 위임자의 동일한 유효 직접 권한까지 재검증 | 구현 완료 |
| 연쇄 위임 | 명시적 차단 기준이 약함 | 위임 권한을 다른 위임의 원천으로 인정하지 않음 | 구현 완료 |
| 메뉴 | 권한 메뉴와 공용 메뉴를 계층 조회 | 공용·권한 메뉴 합산 후 표시 가능한 조상 보존 | 구현 완료 |
| 상위 메뉴 | 계층 쿼리로 포함 | 표시용 상위 폴더와 실제 접근 권한을 분리 | 구현 완료 |
| 버튼 권한 | 클라이언트 `authIds`로 상세 권한 조회 | 서버가 인증 사용자 권한을 다시 계산 | 구현 완료 |
| 기능 문맥 | 권한·메뉴·프로그램·상세 매핑 | `menuId + programId + actionId` 전체 일치 검사 | 구현 완료 |
| 서버 인가 | UI 버튼 제어 중심 경로 존재 | 업무 API마다 `@PreAuthorize` 재검증 | 구현 완료 |
| Nexacro | Dataset, XFDL, 공통 Transaction | 표준 HTML, JSON, Fetch, Spring MVC | 구현 완료 |
| 초기 화면 | 여러 전역 Dataset 구성 | `/api/bootstrap` 단일 초기 응답 | 구현 완료 |
| 쓰기 요청 | Nexacro 공통 트랜잭션 | 업무 권한과 CSRF를 모두 검사 | 구현 완료 |
| 오류 | Nexacro 결과 코드·메시지 | HTTP 상태, 공통 오류 코드, 필드 오류, traceId | 구현 완료 |
| 관심 메뉴 | INFO 공통 기능과 별도 흐름 | 접근 가능 메뉴 재검증 후 등록·조회·삭제 | 구현 완료 |
| 공통코드 | 그룹·상세·하위 코드 테이블 | 계층·활성·적용 기간·그룹 버전·캐시 | 구현 완료·인메모리 |
| 감사 | 권한 관리 이력 등 개별 기록 | 로그인·실패·거부·권한·메뉴·관심 메뉴·공통코드 공통 이벤트 | 구현 완료·인메모리 |
| 테스트 | 화면·통합 환경 의존 비중 큼 | 서비스·보안·로그인·사용자 포털 흐름 자동 테스트 40개 | 구현 완료 |
| 사용자 화면 | Nexacro XFDL과 전역 Dataset 기반 | 반응형 HTML 사용자 홈, 권한 메뉴·버튼·공통코드 반영, 관리 변경 자동 동기화 | 구현 완료·데모 |
| Oracle | 실제 Oracle Mapper와 테이블 사용 | `PM_` 13개 테이블 물리 설계 | 설계 완료 |
| 권한 관리 | 부여·회수 및 매핑 관리 존재 | 직접·위임 부여, 유형별 회수, 현재 효력·전체 이력 관리 화면 | 구현 완료·인메모리 |
| 메뉴 관리 | 권한 중심·메뉴 중심 저장 경로 존재 | 단일 `AdminService`로 메뉴 속성·권한-메뉴 매핑 저장 | 구현 완료·인메모리 |
| SSO | 사내 SSO 연계 | 데모 폼 로그인만 제공 | 단순화 |
| 운영 캐시 | 기존 시스템 공통 처리 사용 | 버전 기반 다중 인스턴스 캐시 설계 필요 | 미구현 |

## 5. 인증과 사용자 신뢰 경계

### 5.1 INFO 기존 내용

INFO는 SSO 인증 이후 공통 Spring 역할을 부여하고 실제 업무 사용자 정보는 `SsoUserDetails`와 세션 `LoginVO`에서 관리합니다.

주요 근거:

- `C:\arisuinfo\src\main\java\arisuinfo\com\security\SsoAuthFilter.java:143`
- `C:\arisuinfo\src\main\java\arisuinfo\com\security\SsoAuthFilter.java:169`
- `C:\arisuinfo\src\main\java\arisuinfo\com\util\SessionUtil.java:38`

즉 Spring 역할과 실제 업무 권한은 원래부터 서로 다른 개념입니다.

### 5.2 보강 내용

독립 프로젝트는 이 분리를 다음처럼 명확하게 구현했습니다.

- Spring 역할 `ROLE_AUTHENTICATED_USER`는 로그인 여부만 표현합니다.
- 업무 권한 ID는 Spring 역할이나 요청 파라미터에서 받지 않습니다.
- `Authentication.getName()`으로 서버 권한 원장을 조회합니다.
- 조직 ID도 요청에서 받지 않고 서버 사용자 프로필에서 결정합니다.
- 익명 또는 알 수 없는 사용자는 권한이 없는 상태로 처리합니다.

### 5.3 현재 제한

- 실제 SSO 대신 로컬 폼 로그인과 인메모리 계정을 사용합니다.
- 사업소·부서·직급 등 INFO의 조직 범위를 단일 `organizationId`로 단순화했습니다.
- Oracle 전환 시 현재 조직의 최종 기준을 SSO·인사 시스템과 DB 중 어디로 할지 결정해야 합니다.

## 6. 직접 권한과 최신 이력

### 6.1 INFO 기존 내용

INFO의 사용자 권한은 다음 테이블 관계를 사용합니다.

| INFO 테이블 | 역할 |
|---|---|
| `TN_CM_AUTH_CLSF_BASIC` | 권한 분류 계층 |
| `TN_CM_AUTH_BASIC` | 권한 마스터와 사용 여부 |
| `TN_CM_AUTH_USR_DTLS` | 사용자 권한 신청·승인·회수 이력 |
| `TN_CM_AUTH_MNDT_DTLS` | 권한 위임 상세 |

로그인 시 사용자·현재 조직·최신 상태·승인·유효 기간·시스템 범위를 사용해 직접 권한을 계산합니다.

### 6.2 확인된 제약

권한 마스터의 `USE_FLAG`가 별도로 존재하지만 유효 사용자 권한 조회의 모든 경로에서 활성 상태가 최종 필터로 명확히 적용되지 않을 여지가 있습니다.

### 6.3 보강 내용

`EffectiveAuthorityService`는 다음 조건을 모두 통과한 권한만 반환합니다.

1. 서버가 확인한 로그인 사용자
2. 서버 사용자 프로필의 현재 조직
3. 사용자·조직·권한·부여 유형별 최신 순번
4. 최신 상태가 `APPROVED`
5. 기준일이 시작일과 종료일 사이
6. 권한 마스터가 존재
7. 권한 마스터가 활성 상태

과거 승인 이력이 있어도 더 최신의 `REVOKED` 이력이 있으면 권한은 제외됩니다. 이전 조직에 남아 있는 승인 권한도 현재 권한으로 인정하지 않습니다.

### 6.4 상태

- 인메모리 계산: 구현 완료
- 자동 테스트: 구현 완료
- Oracle 최신 이력 SQL: 설계 완료, Mapper 미구현

## 7. 위임 권한

### 7.1 INFO 기존 내용

INFO는 피위임자와 위임 기간이 유효한 권한을 직접 권한 결과와 합산합니다.

주요 근거:

- `C:\arisuinfo\src\main\resources\mappers\arisuinfo\cm\auth\auth\authUsrMapper.xml:855`
- `C:\arisuinfo\src\main\resources\mappers\arisuinfo\cm\auth\auth\authUsrMapper.xml:876`

### 7.2 확인된 제약

- 위임자가 해당 권한을 실제 보유하는지 서버 검증이 충분하지 않습니다.
- 위임 이후 원천 권한이 회수돼도 위임 효력이 남을 수 있습니다.
- 위임 권한을 다시 위임하는 연쇄 관계가 명확히 차단되지 않을 수 있습니다.
- 권한별 제한과 위임자 전체 제한의 의미가 섞일 수 있습니다.

### 7.3 보강 내용

독립 프로젝트는 다음 조건을 모두 확인합니다.

- 위임 상태가 승인 상태인지 확인
- 위임 시작일과 종료일 확인
- 위임 권한 마스터 활성 확인
- 위임자와 수임자의 현재 조직 일치 확인
- 위임자가 동일 권한의 유효한 직접 권한을 보유하는지 확인
- 위임자의 원천 직접 권한이 회수되면 수임자 위임 제외
- 다른 위임 권한을 원천으로 인정하지 않아 연쇄 위임 차단

### 7.4 상태

- 런타임 판정: 구현 완료
- 만료·대기·원천 회수 테스트: 구현 완료
- 위임 등록·회수 관리 화면과 API: 구현 완료·인메모리
- Oracle 트랜잭션과 영속 이력 저장: 미구현

## 8. 메뉴 계산

### 8.1 INFO 기존 내용

INFO 로그인 시 다음 흐름으로 메뉴를 계산합니다.

```text
MainMngController.login
  → AuthListServiceImpl.searchAutList
  → AuthUserService.selectLoginAuthUsrInfo
  → 유효 권한 ID 추출
  → AuthMenuService.selectMenuListWithPublic
  → 메뉴 ID 중복 제거
  → gdsAut / gdsMenu 반환
```

권한별 메뉴와 공용 메뉴를 합치고 계층 및 정렬 순서를 유지합니다.

### 8.2 보강 내용

독립 프로젝트는 다음 단계를 서버 서비스로 분리했습니다.

1. 활성 공용 메뉴를 포함합니다.
2. 사용자의 모든 유효 권한에 연결된 메뉴를 합칩니다.
3. 비활성 메뉴를 제거합니다.
4. 화면 메뉴에서는 `displayed=false` 메뉴를 제거합니다.
5. 허용된 자식 메뉴의 표시 가능한 모든 조상을 보존합니다.
6. 부모·자식 구조를 정렬된 불변 트리로 반환합니다.
7. 순환 참조가 있으면 기동 또는 처리 중 예외로 차단합니다.

### 8.3 핵심 차이

상위 폴더가 화면에 보이는 것과 그 메뉴에 직접 접근할 수 있는 것은 별개입니다. 상위 컨테이너는 탐색을 위해 표시할 수 있지만 기능 실행 권한으로 자동 승격되지 않습니다.

### 8.4 상태

- 화면 메뉴 계산과 직접 접근 판정: 구현 완료
- Oracle 메뉴 Mapper: 미구현
- 메뉴 등록·수정·순서 변경 화면과 API: 구현 완료·인메모리
- 권한-메뉴 직접 매핑 변경: 구현 완료·인메모리

## 9. 프로그램 기능과 버튼 권한

### 9.1 INFO 기존 내용

INFO의 프로그램 상세 매핑 논리 키는 다음 네 값입니다.

```text
authId + menuId + pgmId + pgmDetailId
```

Nexacro는 전역 권한 데이터셋의 권한 ID를 서버로 전달하고, 반환된 프로그램 상세 ID와 같은 화면 컴포넌트를 찾아 활성화합니다.

### 9.2 확인된 제약

- 클라이언트가 보낸 `authIds`가 서버 조회조건에 포함됩니다.
- 버튼 비활성화는 UI 제어이므로 업무 API의 실제 인가를 보장하지 않습니다.
- 같은 프로그램을 여러 메뉴에서 사용할 때 메뉴 문맥이 충분히 반영되지 않으면 권한이 섞일 수 있습니다.

### 9.3 보강 내용

- 클라이언트에서 권한 ID를 받지 않습니다.
- 서버가 인증 사용자명으로 유효 권한을 다시 계산합니다.
- `menuId + programId + actionId` 세 값을 전체 문맥으로 검사합니다.
- 기능 마스터가 활성 상태인지 확인합니다.
- 프로그램 마스터가 활성 상태인지 확인합니다.
- 해당 메뉴에 실제 접근할 수 있는지도 확인합니다.
- 화면에는 허용 기능과 컴포넌트 ID만 제공합니다.
- 실제 업무 API에서 같은 키로 `@PreAuthorize`를 수행합니다.
- 관리 화면에서 권한·프로그램·기능 마스터와 권한-기능 매핑을 저장합니다.
- 변경 시 프로그램 버전을 증가시키고 사용자 포털을 다시 구성합니다.

### 9.4 상태

- 조회·저장·게시·관리 조회 재인가: 구현 완료
- 비활성 기능 제외: 구현 완료
- 클라이언트 권한 조작 방지 테스트: 구현 완료
- 권한·프로그램·기능 마스터 등록·수정: 구현 완료
- 실제 권한-기능 허용·해제: 구현 완료

## 10. 권한·메뉴 저장 정합성

### 10.1 INFO 기존 내용

INFO의 권한 중심 저장은 다음 정리를 수행합니다.

- 선택한 하위 메뉴의 조상 추가
- 회수한 메뉴의 프로그램 상세 권한 제거
- 고아 프로그램 권한 제거
- 자식 권한이 없는 상위 메뉴 반복 제거
- 부여·회수 이력 기록

반면 메뉴 중심 저장 경로는 권한 중심 저장과 동일한 정리 및 이력 처리가 적용되지 않는 부분이 있어 관리 진입점에 따라 결과가 달라질 수 있습니다.

### 10.2 보강 내용과 DB 전환 방향

권한 중심 화면과 메뉴 중심 화면이 별도 저장 로직을 갖지 않고 다음 단일 도메인 서비스를 공유해야 합니다.

```text
권한·메뉴 매핑 변경
  → 조상 및 하위 관계 정리
  → 고아 기능 매핑 제거
  → 도메인 버전 증가
  → 감사 이력 기록
  → 하나의 트랜잭션으로 커밋
```

### 10.3 상태

현재 독립 프로젝트는 권한 마스터와 사용자 권한, 메뉴, 프로그램·기능, 권한-메뉴·기능 매핑 변경을 하나의 `AdminService`로 통합했습니다. 변경 시 권한·메뉴·프로그램 버전을 증가시키고 감사 이벤트를 기록하며, 화면은 부트스트랩을 다시 조회해 최종 메뉴와 기능 상태를 맞춥니다.

프로그램 기능 매핑 CRUD는 구현했지만 메뉴 회수 시 하위·고아 기능 매핑 자동 정리는 아직 구현하지 않았습니다. 현재 판정은 메뉴 접근과 기능 매핑을 모두 요구하므로 고아 기능 매핑이 권한을 허용하지는 않지만, Oracle 전환 시 물리 데이터 정리 규칙과 트랜잭션을 추가해야 합니다.

## 11. Nexacro 공통 처리 제거

### 11.1 INFO 기존 내용

INFO는 다음 Nexacro 전용 계약을 사용합니다.

- `NexacroMapDTO`, `DataSetMap`, `DataSetMapConverter`
- `BaseController.processService`, `nexacroMapView`
- `gdsAut`, `gdsMenu`, `gdsCmCd`, `gdsCmCdLwr`
- `dsBtnAuth`, 순번형 출력 Dataset
- Nexacro `ROW_TYPE`
- `arisuTransaction`, `svc::` URL과 콜백
- XFDL 컴포넌트 재귀 탐색

### 11.2 보강 내용

독립 프로젝트는 다음 표준 웹 계약으로 교체했습니다.

| INFO/Nexacro | 독립 프로젝트 |
|---|---|
| Dataset 요청·응답 | JSON 요청·응답 |
| Nexacro Transaction | Fetch API |
| 공통 결과 코드 | HTTP 상태 코드와 공통 오류 봉투 |
| `gdsAut` | 부트스트랩의 유효 권한 목록 |
| `gdsMenu` | 부트스트랩의 메뉴 트리 |
| `dsBtnAuth` | 부트스트랩의 프로그램 기능 목록 |
| XFDL 버튼 검색 | `componentId`와 `data-action` 매핑 |
| Nexacro 세션 흐름 | Spring Security HTTP 세션 |

### 11.3 상태

- Nexacro 없는 독립 실행: 구현 완료
- 자체 로그인·사용자 포털·관리 콘솔 HTML/JavaScript 화면: 구현 완료
- 권한별 메뉴·기능 버튼·공통코드의 사용자 화면 반영: 구현 완료
- 원본 XFDL 전체 화면 대체: 범위 외

## 12. 부트스트랩과 화면 초기화

### 12.1 INFO 기존 내용

INFO는 로그인 결과와 여러 전역 Dataset을 통해 사용자, 권한, 메뉴, 코드 데이터를 구성합니다.

### 12.2 보강 내용

독립 프로젝트의 `/api/bootstrap`은 다음 데이터를 한 번에 제공합니다.

- 사용자명과 현재 조직
- 최종 유효 권한 목록
- 권한별 메뉴 트리
- 전체 허용 기능
- 현재 접근 가능한 관심 메뉴
- 요청한 공통코드 그룹과 버전
- 권한·메뉴·프로그램·공통코드 버전
- 현재 세션 CSRF 정보

여러 API를 순차 호출하면서 화면 데이터가 서로 다른 변경 시점으로 구성되는 문제를 줄이기 위한 구조입니다.

사용자 포털은 권한·메뉴·프로그램·공통코드 버전 키를 15초마다 확인합니다. 변경이 있으면 부트스트랩과 `ARTICLE_STATUS`를 다시 받아 사이드바, 빠른 서비스, 기능 버튼, 상태 배지를 함께 재구성하며 사용자가 수동 새로고침으로 즉시 실행할 수도 있습니다.

### 12.3 현재 제한

인메모리에서는 충분하지만 Oracle 전환 시 유효 권한 계산을 요청당 한 번만 수행하는 `AuthorizationSnapshot`과 읽기 트랜잭션이 추가로 필요합니다.

## 13. 공통코드

### 13.1 INFO 기존 내용

| INFO 테이블 | 역할 |
|---|---|
| `TC_CM_CD` | 공통코드 그룹 |
| `TC_CM_CD_DETAIL` | 상세 코드 |
| `TC_CM_CD_DETAIL_LWR` | 하위 상세 코드 |

로그인 후 전역 Dataset을 이용해 공통코드를 화면에 제공합니다.

### 13.2 보강 내용

독립 프로젝트는 다음 규칙을 서비스 계약으로 명시했습니다.

- 코드와 상위 코드 정규화
- 활성 여부
- 적용 시작일과 종료일
- 상위 코드 존재 검증
- 직접·간접 순환 관계 차단
- 정렬 순서
- 그룹별 변경 버전
- 조회 조건별 캐시
- 저장 성공 후 그룹 캐시 무효화
- 저장 감사 이벤트
- 저장 API의 `COMMON_CODE_SAVE` 권한과 CSRF 검사

### 13.3 상태

- 인메모리 조회·저장·버전·캐시: 구현 완료
- 계층형 지역 코드 화면: 구현 완료
- `ARTICLE_STATUS`의 사용자 홈·콘텐츠 목록 반영: 구현 완료
- Oracle `PM_CODE_GROUP`, `PM_COMMON_CODE`: 설계 완료
- DB 트랜잭션과 다중 서버 캐시: 미구현

## 14. 관심 메뉴

### 14.1 보강 목적

관심 메뉴는 권한과 별개의 사용자 편의 데이터로 정의했습니다.

### 14.2 보강 내용

- 실제 경로가 있는 표시 메뉴만 등록할 수 있습니다.
- 등록 시 현재 접근 권한을 서버가 재검사합니다.
- 조회 시 저장 데이터와 현재 접근 권한의 교집합만 반환합니다.
- 권한 회수 후에는 저장 ID가 남아 있어도 화면에 반환하지 않습니다.
- 등록과 삭제를 감사 이벤트로 기록합니다.

### 14.3 상태

- 인메모리 등록·조회·삭제: 구현 완료
- Oracle 영속 저장: 설계 완료, 미구현

## 15. HTTP 보안과 오류 처리

### 15.1 보강 내용

- 익명 API 요청은 401 JSON으로 반환합니다.
- 권한 없는 요청은 403 JSON으로 반환합니다.
- 상태 변경 요청에 CSRF를 적용합니다.
- Bean Validation 필드 오류를 표준 배열로 반환합니다.
- 내부 예외와 SQL 정보를 사용자 응답에 노출하지 않습니다.
- 모든 오류 응답에 요청 경로와 `traceId`를 제공합니다.
- 세션 쿠키는 HttpOnly와 SameSite를 사용합니다.
- 데모 서버는 `127.0.0.1`에만 바인딩합니다.
- 데모 프로필 외 실행을 차단합니다.

### 15.2 INFO 대비 의미

INFO의 업무 규칙을 화면 버튼 제어에서 끝내지 않고 HTTP API의 최종 보안 경계로 옮긴 것이 핵심입니다.

### 15.3 상태

- 데모 보안: 구현 완료
- Oracle 프로필과 데모 프로필의 분리: 미구현
- 실제 SSO, HTTPS, 프록시 신뢰 설정: 미구현

## 16. 감사 이력

### 16.1 INFO 기존 내용

INFO에는 권한 부여·회수 등 관리 업무 이력이 존재하지만 저장 진입점마다 처리 수준이 다를 수 있습니다.

### 16.2 보강 내용

독립 프로젝트는 다음 사건을 공통 감사 이벤트로 정의했습니다.

- 로그인 성공과 실패
- 로그아웃
- 인증 필요 요청
- 권한 거부
- 관심 메뉴 등록과 삭제
- 공통코드 저장
- 권한 직접·위임 부여와 회수
- 메뉴 등록·수정과 권한-메뉴 매핑 변경

이벤트에는 행위자, 조직, 대상, 결과, IP, traceId, 발생 시각과 민감정보를 제외한 상세를 포함합니다.

### 16.3 상태

- 최근 500건 인메모리 기록과 페이징: 구현 완료
- Oracle `PM_AUDIT_EVENT`: 설계 완료
- 영구 보존·파티셔닝·삭제 권한 통제: 미구현

## 17. 자동 테스트

독립 프로젝트에는 총 40개의 테스트가 있습니다.

| 테스트 영역 | 검증 내용 |
|---|---|
| 유효 권한 | 직접·위임 합산, 만료, 대기, 비활성, 회수, 조직 이동 |
| 위임 원천 | 위임자의 직접 권한 회수 시 수임자 권한 제외 |
| 메뉴 | 공용 메뉴, 조상 보존, 숨김 메뉴, 표시와 접근 분리 |
| 기능 | 조회·저장·게시, 메뉴·프로그램 문맥, 비활성 기능 |
| 공통코드 | 활성 필터, 정렬, 정규화, 계층, 기간, 버전 |
| 권한 관리 | 마스터 등록·수정, 직접 부여·회수의 즉시 반영, 위임 원천 직접 권한 검증 |
| 메뉴 관리 | 등록·권한 매핑 반영, 순환 참조 차단 |
| 프로그램 관리 | 프로그램·기능 등록, 권한-기능 매핑과 실제 사용자 인가 반영 |
| 보안 API | 401, 403, CSRF, 클라이언트 권한 위조 무시 |
| 오류 | 필드 오류와 traceId |
| 세션 | 실제 폼 로그인, CSRF 조회, 로그아웃, 세션 무효화 |
| 관심 메뉴·감사 | 접근권한 재검증, 등록·삭제, 제한 페이징 |

현재 테스트는 인메모리 계약을 검증합니다. Oracle SQL, 잠금, 트랜잭션, 장애 상황은 별도 통합 테스트가 필요합니다.

## 18. DB 설계에서 추가로 보강한 내용

[DB 구축 준비서](DATABASE_PREPARATION.md)는 INFO의 개념을 다음 구조로 재정리했습니다.

- 사용자 현재 조직과 인증 저장소 분리
- 직접·위임 권한 이력을 단일 이력 모델로 정의
- 권한 이력 순번 UNIQUE
- 권한·메뉴·기능 복합 FK
- 계층 메뉴와 공통코드 자기 참조 FK
- Y/N, 상태, 기간 CHECK 제약조건
- 권한·메뉴·기능 도메인 버전
- 공통코드 그룹 버전
- append-only 감사 이력
- 권한과 공통코드 변경의 트랜잭션 경계
- 조회 성능을 위한 인덱스
- 운영·데모·테스트 초기 데이터 분리 필요성

이 항목은 설계 완료 상태이며 실제 Oracle 객체나 Repository는 아직 생성하지 않았습니다.

## 19. INFO보다 아직 부족한 범위

다음 기능은 INFO에는 존재하지만 독립 프로젝트에는 아직 없거나 운영 수준으로 전환되지 않았습니다.

1. 사내 SSO 연계
2. 실제 사용자·사업소·부서 조직 범위
3. Oracle 사용자·권한·메뉴 Mapper
4. 권한 신청·승인·반려·변경·회수 전체 업무
5. 권한 신청·승인·반려를 포함한 결재 화면과 알림
6. 권한 분류 계층과 신청·승인 워크플로
7. 매핑 변경 시 하위·고아 기능 권한 자동 정리
8. 영속 감사 이력과 보존 정책
9. 운영용 다중 서버 캐시 동기화
10. 실제 Oracle 통합 테스트

따라서 독립 프로젝트를 현재 바로 INFO 대체 시스템으로 판단하면 안 됩니다.

## 20. 향후 구현 우선순위

### 1단계: 실행 구조 분리

- 공통 보안 설정과 데모 사용자 설정 분리
- 인증 모드와 저장 모드 분리
- `memory`와 `oracle` 구현을 조건부 빈으로 구성

### 2단계: 저장소 경계 분리

- `AuthorizationCatalog`의 도메인 record 분리
- 사용자·권한·메뉴·기능 Repository 인터페이스 정의
- 인메모리 Adapter로 기존 테스트 유지

### 3단계: 권한 스냅샷

- 한 요청에서 유효 권한을 한 번만 계산
- 메뉴·기능·관심 메뉴가 같은 스냅샷 공유
- Oracle 조회의 N+1 제거

### 4단계: Oracle 읽기 연동

- 사용자 현재 조직
- 최신 직접·위임 권한
- 권한 마스터
- 메뉴와 기능 매핑
- 공통코드와 버전

### 5단계: 관리 쓰기 연동

- 권한 부여·회수·위임
- 메뉴와 기능 매핑
- 공통코드와 관심 메뉴
- 감사 이벤트
- 트랜잭션과 동시성

### 6단계: 운영 검증

- Oracle 통합 테스트
- 성능과 실행계획
- 장애 시 fail-closed
- 다중 서버 캐시
- 감사 보존과 개인정보 통제

## 21. 유지해야 할 핵심 불변식

향후 DB와 SSO를 연결해도 다음 규칙은 변경하지 않습니다.

1. 현재 사용자는 서버 인증 객체에서만 가져옵니다.
2. 조직 범위는 서버의 신뢰 가능한 사용자 정보로 결정합니다.
3. 업무 권한은 최신 승인·기간·활성·조직·위임 원천을 모두 확인합니다.
4. 직접 권한과 위임 권한은 출처를 구분합니다.
5. 위임 권한을 다른 위임의 원천으로 사용하지 않습니다.
6. 여러 유효 권한의 메뉴와 기능은 중복 없는 합집합으로 계산합니다.
7. 메뉴 표시와 실제 메뉴 접근을 구분합니다.
8. 기능 권한은 메뉴·프로그램·기능 전체 문맥으로 확인합니다.
9. UI에서 버튼을 조작해도 서버 업무 API는 다시 인가합니다.
10. 권한이 없거나 데이터가 불완전하면 기본 거부합니다.
11. 변경과 이력·버전 증가는 하나의 트랜잭션으로 처리합니다.
12. 캐시는 DB 커밋 이후 무효화합니다.
13. 비밀번호·세션·CSRF 토큰을 감사 상세에 저장하지 않습니다.
14. 오류 응답에 내부 SQL과 스택 트레이스를 노출하지 않습니다.

## 22. 문서 사용 기준

| 확인 목적 | 기준 문서 |
|---|---|
| INFO 원본 소스와 호출 흐름 | [ARISUINFO 권한 분석](ARISUINFO_PERMISSION_ANALYSIS.md) |
| INFO와 보강 내용 비교 | 현재 문서 |
| 독립 프로젝트 실행 및 구조 | [개발자 매뉴얼](DEVELOPER_MANUAL.md) |
| 사용자 테스트 절차 | [사용자 매뉴얼](USER_MANUAL.md) |
| Oracle 테이블·컬럼·초기 데이터 | [DB 구축 준비서](DATABASE_PREPARATION.md) |

기능을 추가하거나 DB로 전환할 때는 현재 문서의 상태를 `설계 완료` 또는 `미구현`에서 `구현 완료`로 변경하고, 대응 테스트와 근거 파일을 함께 갱신합니다.
