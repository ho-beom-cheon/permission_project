# ARISUINFO 공통 영역 전체 이관 기준

## 목표와 데이터 경계

사용자 요청은 독립 샘플이 아니라 실제 사용할 수 있는 공통 업무 프로그램의 전체 이관이다.
README와 기존 비교 문서의 샘플 목적·의도적 제외 항목은 과거 구현 상태를 설명할 뿐, 앞으로의 제외 근거가 아니다.

- 기준 원본: `C:/arisuinfo`. 이관 대상 프로젝트: `C:/workspace/permission_project`.
- 원본 INFO DB의 실제 데이터를 복사·추출·이관하지 않는다. 원본 DB의 데이터·스키마를 수정하지 않는다.
- 필요할 때 DB 구조 정보만 확인한다. 개발과 테스트는 별도로 작성한 가상 데이터로 수행한다.
- 실제 개인정보, 업무 데이터, 연결 비밀번호를 소스·테스트·문서·로그에 저장하지 않는다.
- 원본 소스는 읽기 전용으로 참조한다. 지금까지 원본 DB에는 접속하지 않았다.
- 기존 인증·감사 기능을 우회하거나 접근 기록을 숨기는 방식으로 작업하지 않는다.

## 이관 범위

| 범위 | 포함 업무 |
|---|---|
| 인증·사용자 | 로그인/SSO 계약, 내 정보, 사용자, 사업소·부서, 담당지역·담당업무, 사용자 모니터링 |
| 권한 | 분류 계층, 권한 마스터, 부여·변경 신청, 개별·일괄 승인/반려/회수, 위임, 이력, 조직·시스템 범위 |
| 메뉴·프로그램 | 메뉴 계층·속성, 프로그램·상세 기능, 양방향 권한 매핑, 연쇄 정리, 실제 API 인가 |
| 기준정보 | 공통코드 그룹·상세·하위, 코드값, 사업소·기관·은행, 휴일·근무일, 원본 bsis의 나머지 항목 |
| 게시판 | 공지·FAQ·Q&A·자료실·게시판, 이용약관과 동의 |
| 운영 | 배치·처리일정·작업·이슈·연계·전자결재·전자고지·발송·다운로드·서식 이력 |
| 사용자 공통 | 초기화, 관심 메뉴, 일정, 업무 알림·처리 이력, 도움말 |
| 공통 기반 | 첨부파일, 주소·코드 팝업, SMS/메일, 메시지·예외·페이징, ID 발급, 세션·보안, 공통 유틸리티 |
| 화면 기반 | Nexacro 화면의 입력·검증·조회·저장·팝업·출력 계약을 HTML/JSON 방식으로 대응 |

`cm`, `com`, `common` 및 해당 Mapper, Nexacro `form/cm`, `common`, `frame`, `arisuInfo` 공통 스크립트를 초기 조사 범위로 잡는다.
폴더 이름만으로 실제 공통팀 업무분장이 확정되는 것은 아니다. 다른 업무 모듈 의존성이 발견되면 제외하지 않고 연계 계약을 추가 조사한다.

## 추적 자료

`python tools/inventory_arisuinfo.py`로 원본을 수정하지 않고 다음 목록을 생성한다.

- `migration/inventory/files.csv`: 파일 경로·인코딩·원본 해시
- `migration/inventory/endpoints.csv`: 매핑 선언 후보와 위치
- `migration/inventory/statements.csv`: Mapper 구문·SQL 조각과 위치
- `migration/inventory/screens.csv`: 화면명과 직접 참조 URL 후보
- `migration/inventory/dependencies.csv`: 내부 참조 및 범위 외 의존 후보
- `migration/inventory/summary.json`: 조사 범위와 집계

최초 집계: 파일 749개, 화면 147개, 매핑 선언 후보 383개, SQL 구문/조각 493개.
이는 기능 수나 이관 완료 수가 아니다. 주석을 제거한 정적 검색이며 동적 호출, 클래스 매핑 조합, XML include 등을 수동 확인해야 한다.
전체 항목은 아직 `NOT_VERIFIED` 상태다. 구현 현황은 별도 대응표에서 관리하며 생성 목록에 완료 여부를 추정해 적지 않는다.

## 완료 기준

1. 원본 기능마다 대응 화면·API·서비스·저장소·검증 항목이 연결된다.
2. 조회·등록·수정·삭제·승인·반려·회수와 업무별 부수 효과를 모두 검증한다.
3. 변경 데이터는 별도 저장소에 영속화하고 재시작 후에도 유지된다.
4. 조직·시스템 범위, 최신 이력, 동시 변경, 일괄 처리 원자성을 검증한다.
5. SSO·메일·문자·전자결재 등은 실제 연계 계약을 구현하고 승인된 시험 환경에서 확인한다.
6. 원본 실제 데이터는 검증용으로 가져오지 않는다. 생성한 가상 데이터로 검증한다.
7. 연결되지 않은 연계와 미구현 업무를 성공 화면·빈 구현으로 대체하지 않는다.

## 현재 남은 외부 조건

원본 DB와 분리된 내장 H2 개발 저장소를 구현했다. SSO·전자결재·발송 등 시험 연계 설정 및 원본과 동등한 별도 운영 스키마는 아직 준비되지 않았다.
원본 INFO DB를 신규 프로그램의 쓰기 대상으로 사용하지 않는다. 연결 설정 없이 원본 DB에 자동 접속하지 않는다.
