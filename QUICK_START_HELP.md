# 권한 관리 데모 빠른 도움말

이 도움말은 화면을 처음 사용하는 사람이 **로그인 → 권한 확인 → 관리 변경 → 결과 확인** 순서로 따라 할 수 있게 만든 요약 안내입니다.
자세한 전체 설명과 오류 목록은 [USER_MANUAL.md](USER_MANUAL.md)를 확인하세요.

## 1. 먼저 실행하기

PowerShell에서 프로젝트 폴더로 이동한 뒤 서버를 실행합니다.

```powershell
cd "C:\workspace\permission_project"
.\mvnw.cmd spring-boot:run
```

`Started PermissionDemoApplication` 메시지가 보이면 브라우저에서 다음 주소를 엽니다.

```text
http://127.0.0.1:8080
```

서버를 멈추려면 실행 중인 PowerShell 창에서 `Ctrl+C`를 누릅니다.

## 2. 어떤 계정으로 시작할까요?

| 목적 | 계정 | 비밀번호 | 확인할 내용 |
|---|---|---|---|
| 모든 관리 기능 확인 | `admin` | `admin123!` | 권한, 메뉴, 프로그램, 공통코드와 감사 이력 관리 |
| 콘텐츠 업무 기능 확인 | `manager` | `manager123!` | 콘텐츠 조회·저장·게시 |
| 권한이 제한된 화면 확인 | `viewer` | `viewer123!` | 콘텐츠 조회만 가능 |
| 위임 권한 확인 | `delegate` | `delegate123!` | 유효한 위임으로 콘텐츠 관리자 기능 사용 |
| 만료 위임 확인 | `expired` | `expired123!` | 만료된 위임은 제외되고 조회만 가능 |

계정은 로컬 데모 전용입니다. 실제 운영 계정이나 비밀번호로 바꾸어 사용하면 안 됩니다.

## 3. 일반 사용자 화면 사용법

로그인하면 사용자 업무 포털이 열립니다.

1. 왼쪽 메뉴에서 사용할 업무를 선택합니다.
2. 홈의 `현재 유효 권한`에서 서버가 계산한 권한을 확인합니다.
3. 콘텐츠 화면에서 활성화된 `조회`, `저장`, `게시` 버튼을 사용합니다.
4. 관리자 변경 후에는 상단 `권한 새로고침`을 눌러 최신 메뉴와 버튼 권한을 받습니다.
5. 자주 쓰는 접근 가능 메뉴는 관심 메뉴로 등록하거나 삭제할 수 있습니다.

버튼이 보이거나 활성화되어도 서버가 최종 권한을 다시 검사합니다. 브라우저 화면을 수정해 비활성 버튼을 눌러도 권한이 없으면 요청은 거부됩니다.

## 4. 관리 콘솔 사용 순서 (`admin`)

`admin`으로 로그인한 뒤 사용자 포털의 `시스템 관리 콘솔`을 누르거나 `http://127.0.0.1:8080/`으로 이동합니다.

권한을 새로 구성할 때는 아래 순서를 권장합니다.

1. **권한 마스터**: 권한 ID와 이름을 등록하거나 활성 상태를 관리합니다.
2. **사용자 권한**: 대상 사용자에게 직접 권한 또는 위임 권한을 부여합니다.
3. **메뉴 관리**: 메뉴를 만들고, 해당 권한에 메뉴 접근을 허용합니다.
4. **프로그램 관리**: 프로그램과 기능을 만들고, 권한에 기능 실행을 허용합니다.
5. 대상 사용자로 다시 로그인하거나 `권한 새로고침`을 눌러 결과를 확인합니다.

기능 실행에는 **사용자 권한 + 메뉴 접근 권한 + 프로그램 기능 권한**이 모두 필요합니다. 기능만 연결하고 메뉴 접근을 허용하지 않으면 기능은 실행되지 않습니다.

## 5. 사용자 권한을 부여하거나 회수하려면

1. 관리 콘솔에서 `시스템 관리 > 사용자 권한`을 엽니다.
2. `대상 사용자`, `권한`, `부여 유형`, 적용 시작일·종료일을 선택합니다.
3. 직접 권한이면 `권한 부여`를 누릅니다.
4. 위임이면 `위임`을 선택하고, 같은 조직에서 해당 권한을 직접 보유한 `위임 원천 사용자`를 입력합니다.
5. 회수는 대상 사용자·권한·유형을 맞춘 뒤 `선택 권한 회수`를 누릅니다.

권한 이력은 덮어쓰지 않습니다. 가장 최근의 부여 또는 회수 이력이 현재 상태가 되며, 만료·승인 대기·비활성 권한은 유효 권한에 포함되지 않습니다.

## 6. 메뉴와 버튼이 보이지 않을 때

다음 순서로 확인합니다.

1. 대상 사용자가 해당 권한을 실제로 보유하는지 확인합니다.
2. 권한이 활성 상태인지, 적용 기간 안인지 확인합니다.
3. 메뉴 관리에서 메뉴가 `활성`이고 `화면 표시` 상태인지 확인합니다.
4. 권한-메뉴 매핑에서 해당 메뉴를 허용했는지 확인합니다.
5. 프로그램 관리에서 프로그램과 기능이 활성 상태인지 확인합니다.
6. 권한-기능 매핑에서 해당 기능을 허용했는지 확인합니다.
7. 사용자 포털에서 `권한 새로고침`을 누르거나 다시 로그인합니다.

상위 메뉴 폴더가 보이는 것은 하위 메뉴를 찾아가기 위한 표시일 수 있습니다. 상위 메뉴가 보인다고 그 메뉴의 업무 실행 권한이 자동으로 생기지는 않습니다.

## 7. 공통코드를 관리하려면

1. `시스템 관리 > 공통코드 관리`를 엽니다.
2. 코드 그룹을 선택합니다. `USE_YN`, `ARTICLE_STATUS`, `AUTH_STATUS`, `REGION`을 예제로 제공합니다.
3. 새 코드라면 코드와 코드명을 입력합니다. 기존 행을 선택하면 수정할 수 있습니다.
4. 필요하면 상위 코드, 정렬 순서, 활성 상태, 시작일·종료일을 지정합니다.
5. `공통코드 저장`을 누르고 목록과 그룹 버전이 갱신되는지 확인합니다.

`REGION`처럼 상위 코드를 지정하면 계층형 코드가 됩니다. 자기 자신을 상위 코드로 지정하거나 순환 구조를 만드는 저장은 거부됩니다.

## 8. 오류가 보일 때

| 상태 | 의미 | 먼저 할 일 |
|---|---|---|
| 400 | 입력값 또는 날짜 형식이 올바르지 않음 | 필수값, 코드 형식, 기간을 확인 |
| 401 | 로그인 세션이 없거나 만료됨 | 다시 로그인 |
| 403 | 권한 부족 또는 CSRF 검증 실패 | 권한 확인 후 화면을 새로 열거나 다시 로그인 |
| 404 | 메뉴·코드·URL을 찾을 수 없음 | 선택한 ID와 경로 확인 |
| 500 | 서버 내부 오류 | 화면의 `traceId`와 발생 시각을 개발자에게 전달 |

오류를 문의할 때는 비밀번호, 세션 쿠키, CSRF 토큰을 공유하지 말고 `traceId`와 수행한 작업만 전달하세요.

## 9. 기억할 점

- 이 데모 데이터는 메모리에만 저장되므로 서버를 재시작하면 초기화됩니다.
- 감사 이력도 재시작 시 사라지며, 실행 중에는 최근 500건까지 보관합니다.
- POST, PUT, DELETE 같은 변경 요청은 화면이 받은 CSRF 토큰을 함께 사용해야 합니다.
- 권한은 화면의 표시가 아니라 서버에서 최종적으로 판단합니다.
