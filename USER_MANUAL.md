# 권한·메뉴·공통코드 테스트 사용자 매뉴얼

## 1. 문서 목적

이 문서는 개발 소스를 수정하지 않고 웹 화면에서 다음 기능을 시험하려는 사용자를 위한 안내서입니다.

- 로그인 사용자별 유효 권한 차이
- 권한에 따라 달라지는 메뉴와 버튼
- 직접 권한과 위임 권한
- 권한 없는 API의 서버 차단
- 관심 메뉴 등록과 삭제
- 공통코드 조회, 계층 선택, 저장과 버전 변경
- 로그인·권한 거부·데이터 변경 감사 이력

이 프로그램은 로컬 테스트 전용입니다. `C:\arisuinfo`와 `C:\arisuccs`에 연결하거나 데이터를 변경하지 않습니다.

## 2. 실행 준비

### 2.1 필수 환경

- Windows 10 또는 11
- Java 17
- Chrome, Edge 등 최신 웹 브라우저
- 인터넷 연결은 최초 Maven 라이브러리 다운로드 시에만 필요할 수 있습니다.

PowerShell에서 Java 버전을 확인합니다.

```powershell
java -version
```

출력에 `17` 버전이 표시되어야 합니다.

### 2.2 개발 모드로 실행

PowerShell을 열고 다음 명령을 실행합니다.

```powershell
cd "C:\Users\이명주\Desktop\cheon\test"
.\mvnw.cmd spring-boot:run
```

콘솔에 `Started PermissionDemoApplication`이 표시되면 브라우저에서 다음 주소를 엽니다.

```text
http://127.0.0.1:8080
```

### 2.3 실행 JAR로 시작

패키징된 파일로 실행하려면 다음 명령을 사용합니다.

```powershell
cd "C:\Users\이명주\Desktop\cheon\test"
java -jar target\permission-menu-demo-1.0.0-SNAPSHOT.jar
```

JAR가 없으면 먼저 빌드합니다.

```powershell
.\mvnw.cmd --batch-mode --no-transfer-progress clean verify
```

### 2.4 종료

서버를 실행한 PowerShell 창에서 `Ctrl+C`를 누릅니다. 창을 닫아도 서버가 종료됩니다.

## 3. 테스트 계정

| 계정 | 비밀번호 | 최종 권한 | 주요 확인 사항 |
|---|---|---|---|
| `admin` | `admin123!` | 시스템 관리자 | 전체 메뉴, 권한 조회, 공통코드 저장, 감사 이력 |
| `manager` | `manager123!` | 콘텐츠 관리자 | 콘텐츠 조회·저장·게시 |
| `viewer` | `viewer123!` | 조회 사용자 | 콘텐츠 조회만 가능, 저장·게시 거부 |
| `delegate` | `delegate123!` | 조회 + 유효한 관리자 위임 | `manager`와 동일한 콘텐츠 기능 |
| `expired` | `expired123!` | 조회 사용자 | 만료된 관리자 위임 제외, 조회만 가능 |

계정과 비밀번호는 로컬 데모 전용입니다. 실제 업무 시스템이나 운영 서버에서 사용하면 안 됩니다.

## 4. 로그인과 로그아웃

### 4.1 로그인

1. `http://127.0.0.1:8080`에 접속합니다.
2. `ARISU Works` 로그인 화면에 계정과 비밀번호를 입력합니다.
3. `로그인` 버튼을 누릅니다.
4. 로그인에 성공하면 `/portal.html` 사용자 업무 포털로 이동합니다.

잘못된 비밀번호를 입력하면 다시 로그인 화면으로 이동하며 로그인 실패 감사 이벤트가 기록됩니다.

### 4.2 로그아웃

1. 화면 오른쪽 위의 `로그아웃` 버튼을 누릅니다.
2. 서버 세션과 `JSESSIONID` 쿠키가 제거됩니다.
3. 로그인 화면으로 이동합니다.

로그아웃은 CSRF 토큰이 포함된 POST 요청으로 처리됩니다. 주소창에서 `/logout`을 직접 여는 방식과 다릅니다.

## 5. 화면 구성과 사용법

### 5.1 사용자 업무 포털

로그인 직후 표시되는 기본 화면은 실제 업무 홈페이지 형태의 사용자 포털입니다.

- 상단: 메뉴 검색, 권한 새로고침, 알림, 사용자·조직, 로그아웃
- 왼쪽: 현재 사용자에게 서버가 허용한 계층 메뉴
- 홈: 유효 권한, 접근 가능 서비스 수, 공통코드 상태, 빠른 서비스, 공지와 일정
- 콘텐츠 관리: 조회·작성·게시 버튼과 `ARTICLE_STATUS` 상태가 적용된 업무 목록
- 시스템 관리 콘솔: 시스템 관리 메뉴가 허용된 계정에게만 표시

화면은 한글 업무 화면을 기준으로 본문 14px 이상, 입력 항목과 주요 버튼 44px 이상의 조작 영역을 사용합니다. 화면 폭이 좁아지면 카드와 입력 항목이 한 열로 재배치되며, 넓은 표는 글자를 지나치게 줄이지 않고 표 하단의 가로 스크롤로 확인할 수 있습니다. 키보드 사용자는 `Tab` 키를 눌러 나타나는 `본문 바로가기` 링크로 반복 메뉴를 건너뛸 수 있습니다.

관리자가 권한·메뉴·공통코드를 저장하면 포털은 화면이 열려 있는 동안 15초마다 변경 버전을 확인합니다. 즉시 확인하려면 상단의 `권한 새로고침` 아이콘을 누릅니다. 변경이 있으면 오른쪽 아래에 반영 안내가 표시됩니다.

### 5.2 서버 계산 메뉴

왼쪽 메뉴는 로그인 계정의 유효 권한을 서버가 계산한 결과입니다.

- `홈`: 모든 로그인 사용자에게 표시됩니다.
- `콘텐츠`: 콘텐츠 하위 메뉴 접근 권한이 있을 때 표시되는 상위 폴더입니다.
- `시스템 관리`: 관리자에게 허용된 시스템 메뉴가 있을 때 표시됩니다.
- 상위 폴더가 보인다고 그 폴더 자체에 업무 기능 권한이 생기는 것은 아닙니다.
- 숨김 메뉴는 접근 권한 데이터가 있어도 화면 메뉴에는 나타나지 않습니다.

브라우저에서 메뉴 HTML을 임의로 만들어도 서버 권한은 증가하지 않습니다.

### 5.3 현재 유효 권한

홈 환영 영역의 권한 칩에는 서버가 계산한 권한 ID가 표시됩니다.

계산에는 다음 조건이 적용됩니다.

- 로그인한 사용자명
- 사용자의 현재 조직
- 권한 승인 또는 회수의 최신 이력
- 권한과 위임의 적용 기간
- 권한 마스터의 활성 상태
- 위임자가 같은 조직에서 원본 직접 권한을 아직 보유하는지 여부

브라우저 요청에 임의의 `authIds`나 `role` 값을 추가해도 이 결과는 바뀌지 않습니다.

### 5.4 프로그램 기능 권한

사용자 포털의 `콘텐츠 조회` 화면에는 다음 버튼이 있습니다.

| 버튼 | 필요한 기능 권한 | 요청 방식 |
|---|---|---|
| 조회 | `CONTENT_READ` | GET |
| 새 콘텐츠 작성 | `CONTENT_SAVE` | POST + CSRF |
| 선택 게시 | `CONTENT_PUBLISH` | POST + CSRF |

허용된 버튼만 활성화됩니다. 하지만 버튼 활성화는 사용자 편의를 위한 표시일 뿐이고, 최종 허용 여부는 서버 API가 다시 검사합니다.

브라우저 개발자 도구로 비활성 상태를 조작해도 권한 없는 API는 HTTP 403으로 차단됩니다.

### 5.5 권한 마스터

권한 마스터는 `admin` 계정으로 관리합니다.

1. 왼쪽 `시스템 관리 > 권한 마스터`를 누릅니다.
2. 기존 행을 누르면 권한 ID, 권한명과 활성 상태가 입력 영역에 표시됩니다.
3. 신규 권한은 `AUTH_`로 시작하는 고유한 영문 대문자 ID와 권한명을 입력합니다.
4. `권한 마스터 저장`을 누릅니다.
5. 신규 권한이 표와 사용자 권한·메뉴·프로그램 매핑 선택지에 나타나는지 확인합니다.

비활성 권한은 기존 사용자 이력과 매핑이 남아 있어도 최종 유효 권한에서 제외됩니다. 전체 관리 기능이 잠기는 것을 방지하기 위해 `AUTH_SYSTEM_ADMIN`은 비활성화할 수 없습니다.

### 5.6 사용자 권한

권한 관리는 `admin` 계정으로 사용합니다.

1. 사용자 포털 왼쪽 아래의 `시스템 관리 콘솔`을 눌러 관리 화면을 엽니다.
2. 관리 화면의 왼쪽 `시스템 관리 > 사용자 권한`을 누릅니다.
3. 표에서 사용자별 현재 유효 권한과 부여·위임·회수 이력을 확인합니다.
4. `대상 사용자`, `권한`, `부여 유형`, `시작일`, `종료일`을 선택합니다.
5. 직접 부여는 `권한 부여`를 바로 누릅니다.
6. 위임은 `부여 유형`을 `위임`으로 바꾸고 해당 권한을 직접 보유한 같은 조직 사용자를 `위임 원천 사용자`로 선택합니다.
7. 회수할 때는 대상·권한·부여 유형을 맞춘 뒤 `선택 권한 회수`를 누릅니다.

변경 성공 후 표가 다시 조회되며 최신 이력이 추가됩니다. 과거 이력을 덮어쓰지 않고 sequence가 큰 행을 최신 상태로 사용합니다. 위임 원천이 다른 조직이거나 직접 권한이 없으면 저장이 거부됩니다.

`감사 이력 조회`를 누르면 권한 부여·회수를 포함한 최근 이벤트를 최신 순으로 최대 20건 확인할 수 있습니다. 관리 메뉴가 없는 계정이 URL을 직접 호출해도 서버가 HTTP 403으로 차단합니다.

### 5.7 메뉴 관리

1. 왼쪽 `시스템 관리 > 메뉴 관리`를 누릅니다.
2. 메뉴 표의 행을 누르면 해당 메뉴의 속성이 아래 입력 영역에 채워집니다.
3. 메뉴 ID, 상위 메뉴, 메뉴명, 화면 경로, 정렬 순서와 활성·표시·공용 여부를 확인하거나 수정합니다.
4. `메뉴 저장`을 누릅니다.
5. 권한과 메뉴를 선택하고 `직접 접근 허용`을 체크하거나 해제합니다.
6. `권한-메뉴 저장`을 누릅니다.
7. 대상 사용자의 포털에서 `권한 새로고침`을 누르고 사이드바와 빠른 서비스에 새 메뉴가 나타나는지 확인합니다.

`#home`과 `#content` 외에 새 경로를 등록하면 사용자 포털이 해당 메뉴명과 접근 허용 상태를 보여주는 기본 업무 화면을 자동 생성합니다. 실제 업무 입력·조회 UI는 이후 해당 경로에 맞춰 확장할 수 있습니다.

화면 경로는 `#authority` 같은 현재 페이지의 앵커 형식만 허용됩니다. 상위 메뉴 자기 참조나 간접 순환 구조는 저장되지 않습니다. `활성`은 접근 자체, `화면 표시`는 사이드바 노출, `공용 메뉴`는 권한 없이 로그인 사용자에게 허용할지를 의미합니다.

### 5.8 프로그램 관리

프로그램 관리는 세 단계로 구성됩니다.

1. `프로그램 마스터`에서 프로그램 ID, 프로그램명, 설명과 활성 상태를 저장합니다.
2. `프로그램 기능`에서 메뉴와 프로그램을 선택하고 기능 ID, 기능명, 선택적 컴포넌트 ID, 정렬 순서와 활성 상태를 저장합니다.
3. `권한-기능 매핑`에서 권한과 기능을 선택합니다.
4. 현재 허용 상태를 확인하고 `실행 허용`을 체크하거나 해제합니다.
5. `권한-기능 저장`을 누릅니다.
6. 같은 권한에 해당 메뉴도 연결되어 있는지 메뉴 관리에서 확인합니다.
7. 대상 사용자의 포털에서 `권한 새로고침`을 누르고 기능 버튼 또는 기능 API 결과를 확인합니다.

기능은 `메뉴 ID + 프로그램 ID + 기능 ID` 전체 조합으로 판정합니다. 권한-기능만 연결하고 권한-메뉴를 연결하지 않으면 실행할 수 없습니다. 비활성 프로그램이나 비활성 기능도 매핑과 무관하게 거부됩니다.

### 5.9 관심 메뉴

1. `등록 후보`에서 메뉴를 선택합니다.
2. `관심 메뉴 등록`을 누릅니다.
3. 등록된 메뉴가 `현재 관심 메뉴`에 표시되는지 확인합니다.
4. 삭제할 메뉴를 선택하고 `관심 메뉴 삭제`를 누릅니다.

관심 메뉴는 즐겨찾기 성격의 편의 정보입니다.

- 현재 접근 가능한 실제 경로 메뉴만 후보가 됩니다.
- 등록 시 서버가 메뉴 접근 권한을 다시 검사합니다.
- 이후 권한이 사라지면 저장 데이터가 있더라도 조회 목록에서 제외됩니다.
- 서버를 재시작하면 관심 메뉴 데이터가 초기화됩니다.

### 5.10 공통코드 조회

왼쪽 `시스템 관리 > 공통코드 관리`를 누른 뒤 `코드 그룹`에서 관리할 그룹을 선택합니다. 선택한 그룹은 비활성 항목까지 표에 표시됩니다.

- `ARTICLE_STATUS`, `AUTH_STATUS`, `REGION`, `USE_YN` 그룹을 선택할 수 있습니다.
- 행에는 코드, 코드명, 상위 코드, 정렬 순서, 활성 상태와 적용 기간이 표시됩니다.
- 그룹명 옆 버전은 해당 그룹의 변경 횟수를 나타냅니다.
- 업무 입력용 활성 조회 API에서는 비활성 코드나 적용 기간 밖의 코드가 제외됩니다.

### 5.11 공통코드 저장

공통코드 저장은 `admin` 계정만 가능합니다.

1. 코드 그룹을 선택합니다.
2. 새 코드는 코드와 코드명을 입력하고, 수정은 표의 행을 눌러 기존 값을 불러옵니다.
3. 선택적으로 상위 코드, 정렬 순서, 활성 상태와 시작일·종료일을 지정합니다.
4. `공통코드 저장`을 누릅니다.
5. 표가 다시 조회되고 선택 그룹의 버전이 증가했는지 확인합니다.

`ARTICLE_STATUS`의 활성 코드명은 사용자 포털 홈의 `업무 상태 코드`와 콘텐츠 목록 상태 배지에 사용됩니다. 저장 후 사용자 포털에서 `권한 새로고침`을 누르면 새 이름과 버전이 함께 반영됩니다.

코드는 서버에서 공백 제거와 대문자 변환 후 저장됩니다. 같은 코드로 다시 저장하면 기존 항목이 갱신됩니다.

### 5.12 계층형 지역 코드

`시·도`를 바꾸면 해당 상위 코드에 속한 `시·군·구`만 표시됩니다.

| 시·도 | 표시되는 시·군·구 |
|---|---|
| 서울 | 강남구, 마포구 |
| 부산 | 해운대구 |

이 기능은 공통코드의 `parentCode` 계층 관계를 확인하기 위한 예제입니다.

### 5.13 API 실행 결과

화면 아래의 `API 실행 결과`에는 다음 정보가 표시됩니다.

- 수행한 기능명
- 성공 응답 데이터
- HTTP 상태 코드가 포함된 실패 제목
- 사용자에게 보여줄 오류 메시지
- 서버 로그와 연결할 수 있는 `traceId`

오류 문의 시 비밀번호나 세션 쿠키 대신 `traceId`와 수행 시각을 전달합니다.

## 6. 계정별 권장 테스트 시나리오

### 6.1 조회 사용자

1. `viewer`로 로그인합니다.
2. 사용자 포털에 홈과 콘텐츠 조회만 표시되고 시스템 관리 콘솔이 보이지 않는지 확인합니다.
3. 콘텐츠 관리에서 조회는 활성, 새 콘텐츠 작성과 선택 게시는 비활성인지 확인합니다.
4. 관리자가 콘텐츠 관리자 권한을 부여한 뒤 `권한 새로고침`으로 두 쓰기 버튼이 활성화되는지 확인합니다.

### 6.2 콘텐츠 관리자

1. `manager`로 로그인합니다.
2. 콘텐츠 조회·저장·게시 버튼이 모두 활성화되는지 확인합니다.
3. 세 버튼을 각각 실행하고 성공 응답을 확인합니다.
4. 시스템 관리 메뉴가 표시되지 않는지 확인합니다.

### 6.3 유효 위임

1. `delegate`로 로그인합니다.
2. 유효 권한에 조회 권한과 콘텐츠 관리자 권한이 함께 표시되는지 확인합니다.
3. 콘텐츠 저장과 게시가 성공하는지 확인합니다.

### 6.4 만료 위임

1. `expired`로 로그인합니다.
2. 만료된 콘텐츠 관리자 위임이 표시되지 않는지 확인합니다.
3. 콘텐츠 조회만 가능하고 게시 강제 호출은 403인지 확인합니다.

### 6.5 시스템 관리자

1. `admin`으로 로그인합니다.
2. 콘텐츠와 시스템 관리 메뉴 및 `시스템 관리 콘솔` 링크가 모두 표시되는지 확인합니다.
3. 관리 콘솔로 이동해 권한 마스터를 조회합니다.
4. 공통코드를 저장하고 버전 증가를 확인합니다.
5. 관심 메뉴를 등록 또는 삭제합니다.
6. 감사 이력을 조회해 로그인과 변경 이벤트를 확인합니다.

## 7. 오류 상태 이해하기

| HTTP 상태 | 의미 | 사용자 조치 |
|---|---|---|
| 400 | 입력값 형식 또는 필드 검증 실패 | 코드, 코드명, 정렬 순서, 기간을 확인합니다. |
| 401 | 로그인하지 않았거나 세션 만료 | 다시 로그인합니다. |
| 403 | 업무 권한 부족 또는 CSRF 검증 실패 | 계정 권한을 확인하고 화면을 새로 열어 다시 시도합니다. |
| 404 | 코드 그룹, 메뉴 또는 리소스 없음 | 입력한 식별자와 URL을 확인합니다. |
| 409 | 현재 데이터 상태에서 처리 불가 | 상위 코드 관계나 중복 상태를 확인합니다. |
| 500 | 예상하지 못한 서버 오류 | 결과의 `traceId`와 콘솔 로그를 개발자에게 전달합니다. |

## 8. 데이터 저장과 초기화

이 프로그램의 데이터는 메모리에 저장됩니다.

- 서버를 재시작하면 추가한 사용자 권한 이력, 메뉴·권한 매핑, 공통코드와 관심 메뉴가 초기 상태로 돌아갑니다.
- 감사 이력도 재시작하면 사라집니다.
- 최근 감사 이력은 실행 중 최대 500건까지만 유지됩니다.
- 실제 DB에 데이터가 저장되거나 원본 프로젝트로 전송되지 않습니다.

완전한 초기화가 필요하면 서버를 종료한 뒤 다시 실행합니다.

## 9. 문제 해결

### 화면에 접속할 수 없음

- PowerShell에 `Started PermissionDemoApplication`이 표시됐는지 확인합니다.
- 주소를 `http://127.0.0.1:8080`으로 입력했는지 확인합니다.
- 다른 프로그램이 8080 포트를 사용 중인지 확인합니다.

```powershell
netstat -ano | findstr :8080
```

### 로그인이 계속 실패함

- 계정과 비밀번호의 영문 대소문자를 확인합니다.
- 복사할 때 앞뒤 공백이 들어가지 않았는지 확인합니다.
- 이 문서의 데모 계정만 로그인할 수 있습니다.

### 버튼이 비활성 상태임

- 해당 계정에 기능 권한이 없는 정상 상태일 수 있습니다.
- 사용자 포털 상단의 `권한 새로고침`을 눌러 부트스트랩 데이터를 다시 조회합니다.
- `API 실행 결과`에 초기화 오류가 있는지 확인합니다.

### 저장 요청이 403임

- `viewer`나 `expired`는 콘텐츠 쓰기 권한이 없습니다.
- 공통코드 저장은 `admin`만 가능합니다.
- 오래 열린 화면이면 세션 또는 CSRF 토큰이 만료됐을 수 있으므로 다시 로그인합니다.

## 10. 사용자 점검표

- [ ] 로그인과 로그아웃이 정상 동작한다.
- [ ] 로그인 후 실제 사용자 업무 포털로 이동한다.
- [ ] 계정별 유효 권한이 예상과 일치한다.
- [ ] 계정별 메뉴 구성이 예상과 일치한다.
- [ ] 허용된 버튼만 활성화된다.
- [ ] 관리자가 변경한 권한·메뉴·공통코드가 사용자 포털 새로고침 후 반영된다.
- [ ] 일반 사용자에게 시스템 관리 콘솔 링크가 표시되지 않는다.
- [ ] 권한 없는 API가 HTTP 403으로 차단된다.
- [ ] 관심 메뉴 등록과 삭제가 동작한다.
- [ ] 상위 지역에 맞는 하위 지역만 표시된다.
- [ ] 공통코드 저장 후 목록과 버전이 함께 갱신된다.
- [ ] 오류 결과에서 `traceId`를 확인할 수 있다.
- [ ] 관리자가 감사 이력을 조회할 수 있다.
